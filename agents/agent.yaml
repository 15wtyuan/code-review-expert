interface:
  display_name: "Code Review Expert"
  short_description: "资深工程师代码审核：SOLID 原则、安全、性能、错误处理"
  default_prompt: "审核当前的 SVN 本地变更，检查 SOLID 原则违反、安全风险、竞态条件、错误处理问题、性能问题和边界条件缺陷。"

# Agent-agnostic skill - works with any LLM provider.
# No provider-specific configuration required.
